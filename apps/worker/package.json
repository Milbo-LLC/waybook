{
  "name": "@waybook/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint configured for @waybook/worker'",
    "test": "echo 'no tests configured for @waybook/worker'"
  },
  "dependencies": {
    "@waybook/db": "workspace:*",
    "bullmq": "^5.40.3",
    "dotenv": "^16.4.7",
    "drizzle-orm": "^0.44.5",
    "ioredis": "5.9.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
