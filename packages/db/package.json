{
  "name": "@waybook/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./schema": {
      "types": "./dist/schema.d.ts",
      "default": "./dist/schema.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint configured for @waybook/db'",
    "test": "echo 'no tests configured for @waybook/db'",
    "db:generate": "DOTENV_CONFIG_PATH=../../.env drizzle-kit generate",
    "db:migrate": "DOTENV_CONFIG_PATH=../../.env drizzle-kit push --force"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.5",
    "pg": "^8.13.3",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.4",
    "@types/pg": "^8.11.11"
  }
}
